// Code generated by https://github.com/gagliardetto/anchor-go. DO NOT EDIT.
// This file contains instructions and instruction parsers.

package pump_fees

import (
	"bytes"
	"fmt"
	errors "github.com/gagliardetto/anchor-go/errors"
	binary "github.com/gagliardetto/binary"
	solanago "github.com/gagliardetto/solana-go"
)

// Builds a "claim_social_fee_pda" instruction.
func NewClaimSocialFeePdaInstruction(
	// Params:
	userIdParam string,
	platformParam uint8,

	// Accounts:
	recipientAccount solanago.PublicKey,
	socialFeePdaAccount solanago.PublicKey,
	feeProgramGlobalAccount solanago.PublicKey,
	socialClaimAuthorityAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_ClaimSocialFeePda[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `userIdParam`:
		err = enc__.Encode(userIdParam)
		if err != nil {
			return nil, errors.NewField("userIdParam", err)
		}
		// Serialize `platformParam`:
		err = enc__.Encode(platformParam)
		if err != nil {
			return nil, errors.NewField("platformParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "recipient": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(recipientAccount, true, false))
		// Account 1 "social_fee_pda": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(socialFeePdaAccount, true, false))
		// Account 2 "fee_program_global": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeProgramGlobalAccount, false, false))
		// Account 3 "social_claim_authority": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(socialClaimAuthorityAccount, false, true))
		// Account 4 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 5 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "crank_donation_fee_pda" instruction.
func NewCrankDonationFeePdaInstruction(
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	associatedTokenProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
	feeProgramGlobalAccount solanago.PublicKey,
	donationFeePdaAccount solanago.PublicKey,
	quoteMintAccount solanago.PublicKey,
	donationFeePdaAtaAccount solanago.PublicKey,
	donationRelayProgramAccount solanago.PublicKey,
	donationRelayEventAuthorityAccount solanago.PublicKey,
	mintWhitelistAccount solanago.PublicKey,
	epochTrackerAccount solanago.PublicKey,
	debouncerAccount solanago.PublicKey,
	debouncerAtaAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CrankDonationFeePda[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 1 "program": Read-only, Non-signer, Required, Address: pfeeUxB6jkeY1Hxd7CsFCAjcbHA9rWtchMGdZ6VojVZ
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
		// Account 2 "payer": Writable, Signer, Required
		// Pays rent when [`temp_wsol_token_account`] is created (`init_if_needed`); receives rent when it is closed after the relay CPI.
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "token_program": Read-only, Non-signer, Required, Address: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 5 "associated_token_program": Read-only, Non-signer, Required, Address: ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL
		accounts__.Append(solanago.NewAccountMeta(associatedTokenProgramAccount, false, false))
		// Account 6 "rent": Read-only, Non-signer, Required, Address: SysvarRent111111111111111111111111111111111
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
		// Account 7 "fee_program_global": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeProgramGlobalAccount, false, false))
		// Account 8 "donation_fee_pda": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(donationFeePdaAccount, true, false))
		// Account 9 "quote_mint": Writable, Non-signer, Required
		// WSOL mint; pubkey fixed by `address` (wrapped SOL native mint).
		accounts__.Append(solanago.NewAccountMeta(quoteMintAccount, true, false))
		// Account 10 "donation_fee_pda_ata": Writable, Non-signer, Required
		// WSOL ATA owned by `donation_fee_pda`.
		accounts__.Append(solanago.NewAccountMeta(donationFeePdaAtaAccount, true, false))
		// Account 11 "donation_relay_program": Read-only, Non-signer, Required, Address: DRLYxueWz6iymdsaRCER6iv6v9zL7gFWANwDL2V5VUx1
		accounts__.Append(solanago.NewAccountMeta(donationRelayProgramAccount, false, false))
		// Account 12 "donation_relay_event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(donationRelayEventAuthorityAccount, false, false))
		// Account 13 "mint_whitelist": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(mintWhitelistAccount, false, false))
		// Account 14 "epoch_tracker": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(epochTrackerAccount, true, false))
		// Account 15 "debouncer": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(debouncerAccount, true, false))
		// Account 16 "debouncer_ata": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(debouncerAtaAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "create_donation_fee_pda" instruction.
func NewCreateDonationFeePdaInstruction(
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	feeProgramGlobalAccount solanago.PublicKey,
	donationFeePdaAccount solanago.PublicKey,
	configIdAccount solanago.PublicKey,
	baseMintAccount solanago.PublicKey,
	bondingCurveAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
	sharingConfigAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CreateDonationFeePda[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 1 "program": Read-only, Non-signer, Required, Address: pfeeUxB6jkeY1Hxd7CsFCAjcbHA9rWtchMGdZ6VojVZ
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
		// Account 2 "payer": Writable, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "fee_program_global": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeProgramGlobalAccount, false, false))
		// Account 5 "donation_fee_pda": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(donationFeePdaAccount, true, false))
		// Account 6 "config_id": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(configIdAccount, false, false))
		// Account 7 "base_mint": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(baseMintAccount, false, false))
		// Account 8 "bonding_curve": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(bondingCurveAccount, false, false))
		// Account 9 "pool": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, false, false))
		// Account 10 "sharing_config": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(sharingConfigAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "create_fee_sharing_config" instruction.
// Create Fee Sharing Config
func NewCreateFeeSharingConfigInstruction(
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	globalAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	sharingConfigAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	bondingCurveAccount solanago.PublicKey,
	pumpProgramAccount solanago.PublicKey,
	pumpEventAuthorityAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
	pumpAmmProgramAccount solanago.PublicKey,
	pumpAmmEventAuthorityAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CreateFeeSharingConfig[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 1 "program": Read-only, Non-signer, Required, Address: pfeeUxB6jkeY1Hxd7CsFCAjcbHA9rWtchMGdZ6VojVZ
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
		// Account 2 "payer": Writable, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "global": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(globalAccount, false, false))
		// Account 4 "mint": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 5 "sharing_config": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(sharingConfigAccount, true, false))
		// Account 6 "system_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 7 "bonding_curve": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(bondingCurveAccount, true, false))
		// Account 8 "pump_program": Read-only, Non-signer, Required, Address: 6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P
		accounts__.Append(solanago.NewAccountMeta(pumpProgramAccount, false, false))
		// Account 9 "pump_event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(pumpEventAuthorityAccount, false, false))
		// Account 10 "pool": Writable, Non-signer, Optional
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
		// Account 11 "pump_amm_program": Read-only, Non-signer, Optional, Address: pAMMBay6oceH9fJKBRHGP5D4bD4sWpmSwMn52FMfXEA
		accounts__.Append(solanago.NewAccountMeta(pumpAmmProgramAccount, false, false))
		// Account 12 "pump_amm_event_authority": Read-only, Non-signer, Optional
		accounts__.Append(solanago.NewAccountMeta(pumpAmmEventAuthorityAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "create_social_fee_pda" instruction.
func NewCreateSocialFeePdaInstruction(
	// Params:
	userIdParam string,
	platformParam uint8,

	// Accounts:
	payerAccount solanago.PublicKey,
	socialFeePdaAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	feeProgramGlobalAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CreateSocialFeePda[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `userIdParam`:
		err = enc__.Encode(userIdParam)
		if err != nil {
			return nil, errors.NewField("userIdParam", err)
		}
		// Serialize `platformParam`:
		err = enc__.Encode(platformParam)
		if err != nil {
			return nil, errors.NewField("platformParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "payer": Writable, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 1 "social_fee_pda": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(socialFeePdaAccount, true, false))
		// Account 2 "system_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 3 "fee_program_global": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeProgramGlobalAccount, false, false))
		// Account 4 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 5 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "get_fees" instruction.
// Get Fees
func NewGetFeesInstruction(
	// Params:
	isPumpPoolParam bool,
	marketCapLamportsParam binary.Uint128,
	tradeSizeLamportsParam uint64,

	// Accounts:
	feeConfigAccount solanago.PublicKey,
	configProgramIdAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_GetFees[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `isPumpPoolParam`:
		err = enc__.Encode(isPumpPoolParam)
		if err != nil {
			return nil, errors.NewField("isPumpPoolParam", err)
		}
		// Serialize `marketCapLamportsParam`:
		err = enc__.Encode(marketCapLamportsParam)
		if err != nil {
			return nil, errors.NewField("marketCapLamportsParam", err)
		}
		// Serialize `tradeSizeLamportsParam`:
		err = enc__.Encode(tradeSizeLamportsParam)
		if err != nil {
			return nil, errors.NewField("tradeSizeLamportsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "fee_config": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeConfigAccount, false, false))
		// Account 1 "config_program_id": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(configProgramIdAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "initialize_fee_config" instruction.
// Initialize FeeConfig admin
func NewInitializeFeeConfigInstruction(
	adminAccount solanago.PublicKey,
	feeConfigAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	configProgramIdAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_InitializeFeeConfig[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "admin": Writable, Signer, Required, Address: 8LWu7QM2dGR1G8nKDHthckea57bkCzXyBTAKPJUBDHo8
		accounts__.Append(solanago.NewAccountMeta(adminAccount, true, true))
		// Account 1 "fee_config": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeConfigAccount, true, false))
		// Account 2 "system_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 3 "config_program_id": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(configProgramIdAccount, false, false))
		// Account 4 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 5 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "initialize_fee_program_global" instruction.
func NewInitializeFeeProgramGlobalInstruction(
	// Params:
	socialClaimAuthorityParam solanago.PublicKey,
	disableFlagsParam uint8,
	claimRateLimitParam uint64,

	// Accounts:
	authorityAccount solanago.PublicKey,
	pumpGlobalAccount solanago.PublicKey,
	feeProgramGlobalAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_InitializeFeeProgramGlobal[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `socialClaimAuthorityParam`:
		err = enc__.Encode(socialClaimAuthorityParam)
		if err != nil {
			return nil, errors.NewField("socialClaimAuthorityParam", err)
		}
		// Serialize `disableFlagsParam`:
		err = enc__.Encode(disableFlagsParam)
		if err != nil {
			return nil, errors.NewField("disableFlagsParam", err)
		}
		// Serialize `claimRateLimitParam`:
		err = enc__.Encode(claimRateLimitParam)
		if err != nil {
			return nil, errors.NewField("claimRateLimitParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Writable, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, true, true))
		// Account 1 "pump_global": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(pumpGlobalAccount, false, false))
		// Account 2 "fee_program_global": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeProgramGlobalAccount, true, false))
		// Account 3 "system_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 5 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "reset_fee_sharing_config" instruction.
// Reset Fee Sharing Config and distribute pending fees first
func NewResetFeeSharingConfigInstruction(
	newAdminAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	globalAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	sharingConfigAccount solanago.PublicKey,
	bondingCurveAccount solanago.PublicKey,
	pumpCreatorVaultAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	pumpProgramAccount solanago.PublicKey,
	pumpEventAuthorityAccount solanago.PublicKey,
	pumpAmmProgramAccount solanago.PublicKey,
	ammEventAuthorityAccount solanago.PublicKey,
	wsolMintAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	associatedTokenProgramAccount solanago.PublicKey,
	coinCreatorVaultAuthorityAccount solanago.PublicKey,
	coinCreatorVaultAtaAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_ResetFeeSharingConfig[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "new_admin": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(newAdminAccount, false, false))
		// Account 1 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 2 "program": Read-only, Non-signer, Required, Address: pfeeUxB6jkeY1Hxd7CsFCAjcbHA9rWtchMGdZ6VojVZ
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
		// Account 3 "authority": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "global": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(globalAccount, false, false))
		// Account 5 "mint": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 6 "sharing_config": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(sharingConfigAccount, true, false))
		// Account 7 "bonding_curve": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(bondingCurveAccount, false, false))
		// Account 8 "pump_creator_vault": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(pumpCreatorVaultAccount, true, false))
		// Account 9 "system_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 10 "pump_program": Read-only, Non-signer, Required, Address: 6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P
		accounts__.Append(solanago.NewAccountMeta(pumpProgramAccount, false, false))
		// Account 11 "pump_event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(pumpEventAuthorityAccount, false, false))
		// Account 12 "pump_amm_program": Read-only, Non-signer, Required, Address: pAMMBay6oceH9fJKBRHGP5D4bD4sWpmSwMn52FMfXEA
		accounts__.Append(solanago.NewAccountMeta(pumpAmmProgramAccount, false, false))
		// Account 13 "amm_event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(ammEventAuthorityAccount, false, false))
		// Account 14 "wsol_mint": Read-only, Non-signer, Required, Address: So11111111111111111111111111111111111111112
		accounts__.Append(solanago.NewAccountMeta(wsolMintAccount, false, false))
		// Account 15 "token_program": Read-only, Non-signer, Required, Address: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 16 "associated_token_program": Read-only, Non-signer, Required, Address: ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL
		accounts__.Append(solanago.NewAccountMeta(associatedTokenProgramAccount, false, false))
		// Account 17 "coin_creator_vault_authority": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(coinCreatorVaultAuthorityAccount, true, false))
		// Account 18 "coin_creator_vault_ata": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(coinCreatorVaultAtaAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "revoke_fee_sharing_authority" instruction.
// Revoke Fee Sharing Authority
func NewRevokeFeeSharingAuthorityInstruction() (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_RevokeFeeSharingAuthority[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "set_authority" instruction.
func NewSetAuthorityInstruction(
	// Params:
	newAuthorityParam solanago.PublicKey,

	// Accounts:
	authorityAccount solanago.PublicKey,
	feeProgramGlobalAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_SetAuthority[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `newAuthorityParam`:
		err = enc__.Encode(newAuthorityParam)
		if err != nil {
			return nil, errors.NewField("newAuthorityParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Writable, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, true, true))
		// Account 1 "fee_program_global": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeProgramGlobalAccount, true, false))
		// Account 2 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 3 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "set_claim_rate_limit" instruction.
func NewSetClaimRateLimitInstruction(
	// Params:
	claimRateLimitParam uint64,

	// Accounts:
	authorityAccount solanago.PublicKey,
	feeProgramGlobalAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_SetClaimRateLimit[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `claimRateLimitParam`:
		err = enc__.Encode(claimRateLimitParam)
		if err != nil {
			return nil, errors.NewField("claimRateLimitParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Writable, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, true, true))
		// Account 1 "fee_program_global": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeProgramGlobalAccount, true, false))
		// Account 2 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 3 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "set_disable_flags" instruction.
func NewSetDisableFlagsInstruction(
	// Params:
	disableFlagsParam uint8,

	// Accounts:
	authorityAccount solanago.PublicKey,
	feeProgramGlobalAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_SetDisableFlags[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `disableFlagsParam`:
		err = enc__.Encode(disableFlagsParam)
		if err != nil {
			return nil, errors.NewField("disableFlagsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Writable, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, true, true))
		// Account 1 "fee_program_global": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeProgramGlobalAccount, true, false))
		// Account 2 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 3 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "set_social_claim_authority" instruction.
func NewSetSocialClaimAuthorityInstruction(
	// Params:
	socialClaimAuthorityParam solanago.PublicKey,

	// Accounts:
	authorityAccount solanago.PublicKey,
	feeProgramGlobalAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_SetSocialClaimAuthority[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `socialClaimAuthorityParam`:
		err = enc__.Encode(socialClaimAuthorityParam)
		if err != nil {
			return nil, errors.NewField("socialClaimAuthorityParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Writable, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, true, true))
		// Account 1 "fee_program_global": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeProgramGlobalAccount, true, false))
		// Account 2 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 3 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "transfer_fee_sharing_authority" instruction.
// Transfer Fee Sharing Authority
func NewTransferFeeSharingAuthorityInstruction() (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_TransferFeeSharingAuthority[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_admin" instruction.
// Update admin (only callable by admin)
func NewUpdateAdminInstruction(
	adminAccount solanago.PublicKey,
	feeConfigAccount solanago.PublicKey,
	newAdminAccount solanago.PublicKey,
	configProgramIdAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateAdmin[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "admin": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(adminAccount, false, true))
		// Account 1 "fee_config": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeConfigAccount, true, false))
		// Account 2 "new_admin": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(newAdminAccount, false, false))
		// Account 3 "config_program_id": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(configProgramIdAccount, false, false))
		// Account 4 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 5 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_fee_config" instruction.
// Set/Replace fee parameters entirely (only callable by admin)
func NewUpdateFeeConfigInstruction(
	// Params:
	feeTiersParam []FeeTier,
	flatFeesParam Fees,

	// Accounts:
	feeConfigAccount solanago.PublicKey,
	adminAccount solanago.PublicKey,
	configProgramIdAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateFeeConfig[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `feeTiersParam`:
		err = enc__.Encode(feeTiersParam)
		if err != nil {
			return nil, errors.NewField("feeTiersParam", err)
		}
		// Serialize `flatFeesParam`:
		err = enc__.Encode(flatFeesParam)
		if err != nil {
			return nil, errors.NewField("flatFeesParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "fee_config": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeConfigAccount, true, false))
		// Account 1 "admin": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(adminAccount, false, true))
		// Account 2 "config_program_id": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(configProgramIdAccount, false, false))
		// Account 3 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 4 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_fee_shares" instruction.
// Update Fee Shares, make sure to distribute all the fees before calling this
func NewUpdateFeeSharesInstruction(
	// Params:
	shareholdersParam []Shareholder,

	// Accounts:
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	globalAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	sharingConfigAccount solanago.PublicKey,
	bondingCurveAccount solanago.PublicKey,
	pumpCreatorVaultAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	pumpProgramAccount solanago.PublicKey,
	pumpEventAuthorityAccount solanago.PublicKey,
	pumpAmmProgramAccount solanago.PublicKey,
	ammEventAuthorityAccount solanago.PublicKey,
	wsolMintAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	associatedTokenProgramAccount solanago.PublicKey,
	coinCreatorVaultAuthorityAccount solanago.PublicKey,
	coinCreatorVaultAtaAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateFeeShares[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `shareholdersParam`:
		err = enc__.Encode(shareholdersParam)
		if err != nil {
			return nil, errors.NewField("shareholdersParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 1 "program": Read-only, Non-signer, Required, Address: pfeeUxB6jkeY1Hxd7CsFCAjcbHA9rWtchMGdZ6VojVZ
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
		// Account 2 "authority": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "global": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(globalAccount, false, false))
		// Account 4 "mint": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 5 "sharing_config": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(sharingConfigAccount, true, false))
		// Account 6 "bonding_curve": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(bondingCurveAccount, false, false))
		// Account 7 "pump_creator_vault": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(pumpCreatorVaultAccount, true, false))
		// Account 8 "system_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 9 "pump_program": Read-only, Non-signer, Required, Address: 6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P
		accounts__.Append(solanago.NewAccountMeta(pumpProgramAccount, false, false))
		// Account 10 "pump_event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(pumpEventAuthorityAccount, false, false))
		// Account 11 "pump_amm_program": Read-only, Non-signer, Required, Address: pAMMBay6oceH9fJKBRHGP5D4bD4sWpmSwMn52FMfXEA
		accounts__.Append(solanago.NewAccountMeta(pumpAmmProgramAccount, false, false))
		// Account 12 "amm_event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(ammEventAuthorityAccount, false, false))
		// Account 13 "wsol_mint": Read-only, Non-signer, Required, Address: So11111111111111111111111111111111111111112
		accounts__.Append(solanago.NewAccountMeta(wsolMintAccount, false, false))
		// Account 14 "token_program": Read-only, Non-signer, Required, Address: TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 15 "associated_token_program": Read-only, Non-signer, Required, Address: ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL
		accounts__.Append(solanago.NewAccountMeta(associatedTokenProgramAccount, false, false))
		// Account 16 "coin_creator_vault_authority": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(coinCreatorVaultAuthorityAccount, true, false))
		// Account 17 "coin_creator_vault_ata": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(coinCreatorVaultAtaAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "upsert_fee_tiers" instruction.
// Update or expand fee tiers (only callable by admin)
func NewUpsertFeeTiersInstruction(
	// Params:
	feeTiersParam []FeeTier,
	offsetParam uint8,

	// Accounts:
	feeConfigAccount solanago.PublicKey,
	adminAccount solanago.PublicKey,
	configProgramIdAccount solanago.PublicKey,
	eventAuthorityAccount solanago.PublicKey,
	programAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpsertFeeTiers[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `feeTiersParam`:
		err = enc__.Encode(feeTiersParam)
		if err != nil {
			return nil, errors.NewField("feeTiersParam", err)
		}
		// Serialize `offsetParam`:
		err = enc__.Encode(offsetParam)
		if err != nil {
			return nil, errors.NewField("offsetParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "fee_config": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(feeConfigAccount, true, false))
		// Account 1 "admin": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(adminAccount, false, true))
		// Account 2 "config_program_id": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(configProgramIdAccount, false, false))
		// Account 3 "event_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(eventAuthorityAccount, false, false))
		// Account 4 "program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(programAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

type ClaimSocialFeePdaInstruction struct {
	UserId   string `json:"user_id"`
	Platform uint8  `json:"platform"`

	// Accounts:
	Recipient                  solanago.PublicKey `json:"recipient"`
	RecipientWritable          bool               `json:"recipient_writable"`
	SocialFeePda               solanago.PublicKey `json:"social_fee_pda"`
	SocialFeePdaWritable       bool               `json:"social_fee_pda_writable"`
	FeeProgramGlobal           solanago.PublicKey `json:"fee_program_global"`
	SocialClaimAuthority       solanago.PublicKey `json:"social_claim_authority"`
	SocialClaimAuthoritySigner bool               `json:"social_claim_authority_signer"`
	EventAuthority             solanago.PublicKey `json:"event_authority"`
	Program                    solanago.PublicKey `json:"program"`
}

func (obj *ClaimSocialFeePdaInstruction) GetDiscriminator() []byte {
	return Instruction_ClaimSocialFeePda[:]
}

// UnmarshalWithDecoder unmarshals the ClaimSocialFeePdaInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *ClaimSocialFeePdaInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "ClaimSocialFeePdaInstruction", err)
	}
	if discriminator != Instruction_ClaimSocialFeePda {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "ClaimSocialFeePdaInstruction", Instruction_ClaimSocialFeePda, discriminator)
	}
	// Deserialize `UserId`:
	err = decoder.Decode(&obj.UserId)
	if err != nil {
		return err
	}
	// Deserialize `Platform`:
	err = decoder.Decode(&obj.Platform)
	if err != nil {
		return err
	}
	return nil
}

func (obj *ClaimSocialFeePdaInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from recipient account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "recipient", err)
	}
	indices = append(indices, index)
	// Decode from social_fee_pda account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "social_fee_pda", err)
	}
	indices = append(indices, index)
	// Decode from fee_program_global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_program_global", err)
	}
	indices = append(indices, index)
	// Decode from social_claim_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "social_claim_authority", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *ClaimSocialFeePdaInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set recipient account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "recipient", len(accountKeys)-1)
	}
	obj.Recipient = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set social_fee_pda account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "social_fee_pda", len(accountKeys)-1)
	}
	obj.SocialFeePda = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_program_global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_program_global", len(accountKeys)-1)
	}
	obj.FeeProgramGlobal = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set social_claim_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "social_claim_authority", len(accountKeys)-1)
	}
	obj.SocialClaimAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *ClaimSocialFeePdaInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Recipient)
	keys = append(keys, obj.SocialFeePda)
	keys = append(keys, obj.FeeProgramGlobal)
	keys = append(keys, obj.SocialClaimAuthority)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the ClaimSocialFeePdaInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *ClaimSocialFeePdaInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling ClaimSocialFeePdaInstruction: %w", err)
	}
	return nil
}

// UnmarshalClaimSocialFeePdaInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalClaimSocialFeePdaInstruction(buf []byte) (*ClaimSocialFeePdaInstruction, error) {
	obj := new(ClaimSocialFeePdaInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CrankDonationFeePdaInstruction struct {

	// Accounts:
	EventAuthority              solanago.PublicKey `json:"event_authority"`
	Program                     solanago.PublicKey `json:"program"`
	Payer                       solanago.PublicKey `json:"payer"`
	PayerWritable               bool               `json:"payer_writable"`
	PayerSigner                 bool               `json:"payer_signer"`
	SystemProgram               solanago.PublicKey `json:"system_program"`
	TokenProgram                solanago.PublicKey `json:"token_program"`
	AssociatedTokenProgram      solanago.PublicKey `json:"associated_token_program"`
	Rent                        solanago.PublicKey `json:"rent"`
	FeeProgramGlobal            solanago.PublicKey `json:"fee_program_global"`
	DonationFeePda              solanago.PublicKey `json:"donation_fee_pda"`
	DonationFeePdaWritable      bool               `json:"donation_fee_pda_writable"`
	QuoteMint                   solanago.PublicKey `json:"quote_mint"`
	QuoteMintWritable           bool               `json:"quote_mint_writable"`
	DonationFeePdaAta           solanago.PublicKey `json:"donation_fee_pda_ata"`
	DonationFeePdaAtaWritable   bool               `json:"donation_fee_pda_ata_writable"`
	DonationRelayProgram        solanago.PublicKey `json:"donation_relay_program"`
	DonationRelayEventAuthority solanago.PublicKey `json:"donation_relay_event_authority"`
	MintWhitelist               solanago.PublicKey `json:"mint_whitelist"`
	EpochTracker                solanago.PublicKey `json:"epoch_tracker"`
	EpochTrackerWritable        bool               `json:"epoch_tracker_writable"`
	Debouncer                   solanago.PublicKey `json:"debouncer"`
	DebouncerWritable           bool               `json:"debouncer_writable"`
	DebouncerAta                solanago.PublicKey `json:"debouncer_ata"`
	DebouncerAtaWritable        bool               `json:"debouncer_ata_writable"`
}

func (obj *CrankDonationFeePdaInstruction) GetDiscriminator() []byte {
	return Instruction_CrankDonationFeePda[:]
}

// UnmarshalWithDecoder unmarshals the CrankDonationFeePdaInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CrankDonationFeePdaInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CrankDonationFeePdaInstruction", err)
	}
	if discriminator != Instruction_CrankDonationFeePda {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "CrankDonationFeePdaInstruction", Instruction_CrankDonationFeePda, discriminator)
	}
	return nil
}

func (obj *CrankDonationFeePdaInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from associated_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "associated_token_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	// Decode from fee_program_global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_program_global", err)
	}
	indices = append(indices, index)
	// Decode from donation_fee_pda account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "donation_fee_pda", err)
	}
	indices = append(indices, index)
	// Decode from quote_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "quote_mint", err)
	}
	indices = append(indices, index)
	// Decode from donation_fee_pda_ata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "donation_fee_pda_ata", err)
	}
	indices = append(indices, index)
	// Decode from donation_relay_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "donation_relay_program", err)
	}
	indices = append(indices, index)
	// Decode from donation_relay_event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "donation_relay_event_authority", err)
	}
	indices = append(indices, index)
	// Decode from mint_whitelist account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint_whitelist", err)
	}
	indices = append(indices, index)
	// Decode from epoch_tracker account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "epoch_tracker", err)
	}
	indices = append(indices, index)
	// Decode from debouncer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "debouncer", err)
	}
	indices = append(indices, index)
	// Decode from debouncer_ata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "debouncer_ata", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CrankDonationFeePdaInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 17 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 17, len(indices))
	}
	indexOffset := 0
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set associated_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "associated_token_program", len(accountKeys)-1)
	}
	obj.AssociatedTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_program_global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_program_global", len(accountKeys)-1)
	}
	obj.FeeProgramGlobal = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set donation_fee_pda account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "donation_fee_pda", len(accountKeys)-1)
	}
	obj.DonationFeePda = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set quote_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "quote_mint", len(accountKeys)-1)
	}
	obj.QuoteMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set donation_fee_pda_ata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "donation_fee_pda_ata", len(accountKeys)-1)
	}
	obj.DonationFeePdaAta = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set donation_relay_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "donation_relay_program", len(accountKeys)-1)
	}
	obj.DonationRelayProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set donation_relay_event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "donation_relay_event_authority", len(accountKeys)-1)
	}
	obj.DonationRelayEventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint_whitelist account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint_whitelist", len(accountKeys)-1)
	}
	obj.MintWhitelist = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set epoch_tracker account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "epoch_tracker", len(accountKeys)-1)
	}
	obj.EpochTracker = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set debouncer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "debouncer", len(accountKeys)-1)
	}
	obj.Debouncer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set debouncer_ata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "debouncer_ata", len(accountKeys)-1)
	}
	obj.DebouncerAta = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CrankDonationFeePdaInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.AssociatedTokenProgram)
	keys = append(keys, obj.Rent)
	keys = append(keys, obj.FeeProgramGlobal)
	keys = append(keys, obj.DonationFeePda)
	keys = append(keys, obj.QuoteMint)
	keys = append(keys, obj.DonationFeePdaAta)
	keys = append(keys, obj.DonationRelayProgram)
	keys = append(keys, obj.DonationRelayEventAuthority)
	keys = append(keys, obj.MintWhitelist)
	keys = append(keys, obj.EpochTracker)
	keys = append(keys, obj.Debouncer)
	keys = append(keys, obj.DebouncerAta)
	return keys
}

// Unmarshal unmarshals the CrankDonationFeePdaInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CrankDonationFeePdaInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CrankDonationFeePdaInstruction: %w", err)
	}
	return nil
}

// UnmarshalCrankDonationFeePdaInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCrankDonationFeePdaInstruction(buf []byte) (*CrankDonationFeePdaInstruction, error) {
	obj := new(CrankDonationFeePdaInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateDonationFeePdaInstruction struct {

	// Accounts:
	EventAuthority         solanago.PublicKey `json:"event_authority"`
	Program                solanago.PublicKey `json:"program"`
	Payer                  solanago.PublicKey `json:"payer"`
	PayerWritable          bool               `json:"payer_writable"`
	PayerSigner            bool               `json:"payer_signer"`
	SystemProgram          solanago.PublicKey `json:"system_program"`
	FeeProgramGlobal       solanago.PublicKey `json:"fee_program_global"`
	DonationFeePda         solanago.PublicKey `json:"donation_fee_pda"`
	DonationFeePdaWritable bool               `json:"donation_fee_pda_writable"`
	ConfigId               solanago.PublicKey `json:"config_id"`
	BaseMint               solanago.PublicKey `json:"base_mint"`
	BondingCurve           solanago.PublicKey `json:"bonding_curve"`
	Pool                   solanago.PublicKey `json:"pool"`
	SharingConfig          solanago.PublicKey `json:"sharing_config"`
}

func (obj *CreateDonationFeePdaInstruction) GetDiscriminator() []byte {
	return Instruction_CreateDonationFeePda[:]
}

// UnmarshalWithDecoder unmarshals the CreateDonationFeePdaInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateDonationFeePdaInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateDonationFeePdaInstruction", err)
	}
	if discriminator != Instruction_CreateDonationFeePda {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "CreateDonationFeePdaInstruction", Instruction_CreateDonationFeePda, discriminator)
	}
	return nil
}

func (obj *CreateDonationFeePdaInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from fee_program_global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_program_global", err)
	}
	indices = append(indices, index)
	// Decode from donation_fee_pda account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "donation_fee_pda", err)
	}
	indices = append(indices, index)
	// Decode from config_id account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "config_id", err)
	}
	indices = append(indices, index)
	// Decode from base_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "base_mint", err)
	}
	indices = append(indices, index)
	// Decode from bonding_curve account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "bonding_curve", err)
	}
	indices = append(indices, index)
	// Decode from pool account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pool", err)
	}
	indices = append(indices, index)
	// Decode from sharing_config account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sharing_config", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateDonationFeePdaInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 11 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 11, len(indices))
	}
	indexOffset := 0
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_program_global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_program_global", len(accountKeys)-1)
	}
	obj.FeeProgramGlobal = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set donation_fee_pda account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "donation_fee_pda", len(accountKeys)-1)
	}
	obj.DonationFeePda = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set config_id account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "config_id", len(accountKeys)-1)
	}
	obj.ConfigId = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set base_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "base_mint", len(accountKeys)-1)
	}
	obj.BaseMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set bonding_curve account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "bonding_curve", len(accountKeys)-1)
	}
	obj.BondingCurve = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pool account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pool", len(accountKeys)-1)
	}
	obj.Pool = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sharing_config account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sharing_config", len(accountKeys)-1)
	}
	obj.SharingConfig = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateDonationFeePdaInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.FeeProgramGlobal)
	keys = append(keys, obj.DonationFeePda)
	keys = append(keys, obj.ConfigId)
	keys = append(keys, obj.BaseMint)
	keys = append(keys, obj.BondingCurve)
	keys = append(keys, obj.Pool)
	keys = append(keys, obj.SharingConfig)
	return keys
}

// Unmarshal unmarshals the CreateDonationFeePdaInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateDonationFeePdaInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateDonationFeePdaInstruction: %w", err)
	}
	return nil
}

// UnmarshalCreateDonationFeePdaInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateDonationFeePdaInstruction(buf []byte) (*CreateDonationFeePdaInstruction, error) {
	obj := new(CreateDonationFeePdaInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateFeeSharingConfigInstruction struct {

	// Accounts:
	EventAuthority                solanago.PublicKey `json:"event_authority"`
	Program                       solanago.PublicKey `json:"program"`
	Payer                         solanago.PublicKey `json:"payer"`
	PayerWritable                 bool               `json:"payer_writable"`
	PayerSigner                   bool               `json:"payer_signer"`
	Global                        solanago.PublicKey `json:"global"`
	Mint                          solanago.PublicKey `json:"mint"`
	SharingConfig                 solanago.PublicKey `json:"sharing_config"`
	SharingConfigWritable         bool               `json:"sharing_config_writable"`
	SystemProgram                 solanago.PublicKey `json:"system_program"`
	BondingCurve                  solanago.PublicKey `json:"bonding_curve"`
	BondingCurveWritable          bool               `json:"bonding_curve_writable"`
	PumpProgram                   solanago.PublicKey `json:"pump_program"`
	PumpEventAuthority            solanago.PublicKey `json:"pump_event_authority"`
	Pool                          solanago.PublicKey `json:"pool"`
	PoolWritable                  bool               `json:"pool_writable"`
	PoolOptional                  bool               `json:"pool_optional"`
	PumpAmmProgram                solanago.PublicKey `json:"pump_amm_program"`
	PumpAmmProgramOptional        bool               `json:"pump_amm_program_optional"`
	PumpAmmEventAuthority         solanago.PublicKey `json:"pump_amm_event_authority"`
	PumpAmmEventAuthorityOptional bool               `json:"pump_amm_event_authority_optional"`
}

func (obj *CreateFeeSharingConfigInstruction) GetDiscriminator() []byte {
	return Instruction_CreateFeeSharingConfig[:]
}

// UnmarshalWithDecoder unmarshals the CreateFeeSharingConfigInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateFeeSharingConfigInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateFeeSharingConfigInstruction", err)
	}
	if discriminator != Instruction_CreateFeeSharingConfig {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "CreateFeeSharingConfigInstruction", Instruction_CreateFeeSharingConfig, discriminator)
	}
	return nil
}

func (obj *CreateFeeSharingConfigInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "global", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from sharing_config account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sharing_config", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from bonding_curve account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "bonding_curve", err)
	}
	indices = append(indices, index)
	// Decode from pump_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_program", err)
	}
	indices = append(indices, index)
	// Decode from pump_event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_event_authority", err)
	}
	indices = append(indices, index)
	// Decode from pool account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pool", err)
	}
	indices = append(indices, index)
	// Decode from pump_amm_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_amm_program", err)
	}
	indices = append(indices, index)
	// Decode from pump_amm_event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_amm_event_authority", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateFeeSharingConfigInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 13 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 13, len(indices))
	}
	indexOffset := 0
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "global", len(accountKeys)-1)
	}
	obj.Global = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sharing_config account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sharing_config", len(accountKeys)-1)
	}
	obj.SharingConfig = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set bonding_curve account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "bonding_curve", len(accountKeys)-1)
	}
	obj.BondingCurve = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_program", len(accountKeys)-1)
	}
	obj.PumpProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_event_authority", len(accountKeys)-1)
	}
	obj.PumpEventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pool account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pool", len(accountKeys)-1)
	}
	obj.Pool = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_amm_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_amm_program", len(accountKeys)-1)
	}
	obj.PumpAmmProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_amm_event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_amm_event_authority", len(accountKeys)-1)
	}
	obj.PumpAmmEventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateFeeSharingConfigInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Global)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.SharingConfig)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.BondingCurve)
	keys = append(keys, obj.PumpProgram)
	keys = append(keys, obj.PumpEventAuthority)
	keys = append(keys, obj.Pool)
	keys = append(keys, obj.PumpAmmProgram)
	keys = append(keys, obj.PumpAmmEventAuthority)
	return keys
}

// Unmarshal unmarshals the CreateFeeSharingConfigInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateFeeSharingConfigInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateFeeSharingConfigInstruction: %w", err)
	}
	return nil
}

// UnmarshalCreateFeeSharingConfigInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateFeeSharingConfigInstruction(buf []byte) (*CreateFeeSharingConfigInstruction, error) {
	obj := new(CreateFeeSharingConfigInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateSocialFeePdaInstruction struct {
	UserId   string `json:"user_id"`
	Platform uint8  `json:"platform"`

	// Accounts:
	Payer                solanago.PublicKey `json:"payer"`
	PayerWritable        bool               `json:"payer_writable"`
	PayerSigner          bool               `json:"payer_signer"`
	SocialFeePda         solanago.PublicKey `json:"social_fee_pda"`
	SocialFeePdaWritable bool               `json:"social_fee_pda_writable"`
	SystemProgram        solanago.PublicKey `json:"system_program"`
	FeeProgramGlobal     solanago.PublicKey `json:"fee_program_global"`
	EventAuthority       solanago.PublicKey `json:"event_authority"`
	Program              solanago.PublicKey `json:"program"`
}

func (obj *CreateSocialFeePdaInstruction) GetDiscriminator() []byte {
	return Instruction_CreateSocialFeePda[:]
}

// UnmarshalWithDecoder unmarshals the CreateSocialFeePdaInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateSocialFeePdaInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateSocialFeePdaInstruction", err)
	}
	if discriminator != Instruction_CreateSocialFeePda {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "CreateSocialFeePdaInstruction", Instruction_CreateSocialFeePda, discriminator)
	}
	// Deserialize `UserId`:
	err = decoder.Decode(&obj.UserId)
	if err != nil {
		return err
	}
	// Deserialize `Platform`:
	err = decoder.Decode(&obj.Platform)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CreateSocialFeePdaInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from social_fee_pda account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "social_fee_pda", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from fee_program_global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_program_global", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateSocialFeePdaInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set social_fee_pda account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "social_fee_pda", len(accountKeys)-1)
	}
	obj.SocialFeePda = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_program_global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_program_global", len(accountKeys)-1)
	}
	obj.FeeProgramGlobal = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateSocialFeePdaInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SocialFeePda)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.FeeProgramGlobal)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the CreateSocialFeePdaInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateSocialFeePdaInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateSocialFeePdaInstruction: %w", err)
	}
	return nil
}

// UnmarshalCreateSocialFeePdaInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateSocialFeePdaInstruction(buf []byte) (*CreateSocialFeePdaInstruction, error) {
	obj := new(CreateSocialFeePdaInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type GetFeesInstruction struct {
	IsPumpPool        bool           `json:"is_pump_pool"`
	MarketCapLamports binary.Uint128 `json:"market_cap_lamports"`
	TradeSizeLamports uint64         `json:"trade_size_lamports"`

	// Accounts:
	FeeConfig       solanago.PublicKey `json:"fee_config"`
	ConfigProgramId solanago.PublicKey `json:"config_program_id"`
}

func (obj *GetFeesInstruction) GetDiscriminator() []byte {
	return Instruction_GetFees[:]
}

// UnmarshalWithDecoder unmarshals the GetFeesInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *GetFeesInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "GetFeesInstruction", err)
	}
	if discriminator != Instruction_GetFees {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "GetFeesInstruction", Instruction_GetFees, discriminator)
	}
	// Deserialize `IsPumpPool`:
	err = decoder.Decode(&obj.IsPumpPool)
	if err != nil {
		return err
	}
	// Deserialize `MarketCapLamports`:
	err = decoder.Decode(&obj.MarketCapLamports)
	if err != nil {
		return err
	}
	// Deserialize `TradeSizeLamports`:
	err = decoder.Decode(&obj.TradeSizeLamports)
	if err != nil {
		return err
	}
	return nil
}

func (obj *GetFeesInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from fee_config account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_config", err)
	}
	indices = append(indices, index)
	// Decode from config_program_id account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "config_program_id", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *GetFeesInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 2 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 2, len(indices))
	}
	indexOffset := 0
	// Set fee_config account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_config", len(accountKeys)-1)
	}
	obj.FeeConfig = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set config_program_id account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "config_program_id", len(accountKeys)-1)
	}
	obj.ConfigProgramId = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *GetFeesInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.FeeConfig)
	keys = append(keys, obj.ConfigProgramId)
	return keys
}

// Unmarshal unmarshals the GetFeesInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *GetFeesInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling GetFeesInstruction: %w", err)
	}
	return nil
}

// UnmarshalGetFeesInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalGetFeesInstruction(buf []byte) (*GetFeesInstruction, error) {
	obj := new(GetFeesInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type InitializeFeeConfigInstruction struct {

	// Accounts:
	Admin             solanago.PublicKey `json:"admin"`
	AdminWritable     bool               `json:"admin_writable"`
	AdminSigner       bool               `json:"admin_signer"`
	FeeConfig         solanago.PublicKey `json:"fee_config"`
	FeeConfigWritable bool               `json:"fee_config_writable"`
	SystemProgram     solanago.PublicKey `json:"system_program"`
	ConfigProgramId   solanago.PublicKey `json:"config_program_id"`
	EventAuthority    solanago.PublicKey `json:"event_authority"`
	Program           solanago.PublicKey `json:"program"`
}

func (obj *InitializeFeeConfigInstruction) GetDiscriminator() []byte {
	return Instruction_InitializeFeeConfig[:]
}

// UnmarshalWithDecoder unmarshals the InitializeFeeConfigInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *InitializeFeeConfigInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "InitializeFeeConfigInstruction", err)
	}
	if discriminator != Instruction_InitializeFeeConfig {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "InitializeFeeConfigInstruction", Instruction_InitializeFeeConfig, discriminator)
	}
	return nil
}

func (obj *InitializeFeeConfigInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from admin account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "admin", err)
	}
	indices = append(indices, index)
	// Decode from fee_config account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_config", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from config_program_id account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "config_program_id", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *InitializeFeeConfigInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set admin account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "admin", len(accountKeys)-1)
	}
	obj.Admin = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_config account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_config", len(accountKeys)-1)
	}
	obj.FeeConfig = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set config_program_id account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "config_program_id", len(accountKeys)-1)
	}
	obj.ConfigProgramId = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *InitializeFeeConfigInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Admin)
	keys = append(keys, obj.FeeConfig)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.ConfigProgramId)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the InitializeFeeConfigInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *InitializeFeeConfigInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling InitializeFeeConfigInstruction: %w", err)
	}
	return nil
}

// UnmarshalInitializeFeeConfigInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalInitializeFeeConfigInstruction(buf []byte) (*InitializeFeeConfigInstruction, error) {
	obj := new(InitializeFeeConfigInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type InitializeFeeProgramGlobalInstruction struct {
	SocialClaimAuthority solanago.PublicKey `json:"social_claim_authority"`
	DisableFlags         uint8              `json:"disable_flags"`
	ClaimRateLimit       uint64             `json:"claim_rate_limit"`

	// Accounts:
	Authority                solanago.PublicKey `json:"authority"`
	AuthorityWritable        bool               `json:"authority_writable"`
	AuthoritySigner          bool               `json:"authority_signer"`
	PumpGlobal               solanago.PublicKey `json:"pump_global"`
	FeeProgramGlobal         solanago.PublicKey `json:"fee_program_global"`
	FeeProgramGlobalWritable bool               `json:"fee_program_global_writable"`
	SystemProgram            solanago.PublicKey `json:"system_program"`
	EventAuthority           solanago.PublicKey `json:"event_authority"`
	Program                  solanago.PublicKey `json:"program"`
}

func (obj *InitializeFeeProgramGlobalInstruction) GetDiscriminator() []byte {
	return Instruction_InitializeFeeProgramGlobal[:]
}

// UnmarshalWithDecoder unmarshals the InitializeFeeProgramGlobalInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *InitializeFeeProgramGlobalInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "InitializeFeeProgramGlobalInstruction", err)
	}
	if discriminator != Instruction_InitializeFeeProgramGlobal {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "InitializeFeeProgramGlobalInstruction", Instruction_InitializeFeeProgramGlobal, discriminator)
	}
	// Deserialize `SocialClaimAuthority`:
	err = decoder.Decode(&obj.SocialClaimAuthority)
	if err != nil {
		return err
	}
	// Deserialize `DisableFlags`:
	err = decoder.Decode(&obj.DisableFlags)
	if err != nil {
		return err
	}
	// Deserialize `ClaimRateLimit`:
	err = decoder.Decode(&obj.ClaimRateLimit)
	if err != nil {
		return err
	}
	return nil
}

func (obj *InitializeFeeProgramGlobalInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from pump_global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_global", err)
	}
	indices = append(indices, index)
	// Decode from fee_program_global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_program_global", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *InitializeFeeProgramGlobalInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_global", len(accountKeys)-1)
	}
	obj.PumpGlobal = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_program_global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_program_global", len(accountKeys)-1)
	}
	obj.FeeProgramGlobal = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *InitializeFeeProgramGlobalInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.PumpGlobal)
	keys = append(keys, obj.FeeProgramGlobal)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the InitializeFeeProgramGlobalInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *InitializeFeeProgramGlobalInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling InitializeFeeProgramGlobalInstruction: %w", err)
	}
	return nil
}

// UnmarshalInitializeFeeProgramGlobalInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalInitializeFeeProgramGlobalInstruction(buf []byte) (*InitializeFeeProgramGlobalInstruction, error) {
	obj := new(InitializeFeeProgramGlobalInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type ResetFeeSharingConfigInstruction struct {

	// Accounts:
	NewAdmin                          solanago.PublicKey `json:"new_admin"`
	EventAuthority                    solanago.PublicKey `json:"event_authority"`
	Program                           solanago.PublicKey `json:"program"`
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	Global                            solanago.PublicKey `json:"global"`
	Mint                              solanago.PublicKey `json:"mint"`
	SharingConfig                     solanago.PublicKey `json:"sharing_config"`
	SharingConfigWritable             bool               `json:"sharing_config_writable"`
	BondingCurve                      solanago.PublicKey `json:"bonding_curve"`
	PumpCreatorVault                  solanago.PublicKey `json:"pump_creator_vault"`
	PumpCreatorVaultWritable          bool               `json:"pump_creator_vault_writable"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	PumpProgram                       solanago.PublicKey `json:"pump_program"`
	PumpEventAuthority                solanago.PublicKey `json:"pump_event_authority"`
	PumpAmmProgram                    solanago.PublicKey `json:"pump_amm_program"`
	AmmEventAuthority                 solanago.PublicKey `json:"amm_event_authority"`
	WsolMint                          solanago.PublicKey `json:"wsol_mint"`
	TokenProgram                      solanago.PublicKey `json:"token_program"`
	AssociatedTokenProgram            solanago.PublicKey `json:"associated_token_program"`
	CoinCreatorVaultAuthority         solanago.PublicKey `json:"coin_creator_vault_authority"`
	CoinCreatorVaultAuthorityWritable bool               `json:"coin_creator_vault_authority_writable"`
	CoinCreatorVaultAta               solanago.PublicKey `json:"coin_creator_vault_ata"`
	CoinCreatorVaultAtaWritable       bool               `json:"coin_creator_vault_ata_writable"`
}

func (obj *ResetFeeSharingConfigInstruction) GetDiscriminator() []byte {
	return Instruction_ResetFeeSharingConfig[:]
}

// UnmarshalWithDecoder unmarshals the ResetFeeSharingConfigInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *ResetFeeSharingConfigInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "ResetFeeSharingConfigInstruction", err)
	}
	if discriminator != Instruction_ResetFeeSharingConfig {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "ResetFeeSharingConfigInstruction", Instruction_ResetFeeSharingConfig, discriminator)
	}
	return nil
}

func (obj *ResetFeeSharingConfigInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from new_admin account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_admin", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "global", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from sharing_config account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sharing_config", err)
	}
	indices = append(indices, index)
	// Decode from bonding_curve account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "bonding_curve", err)
	}
	indices = append(indices, index)
	// Decode from pump_creator_vault account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_creator_vault", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from pump_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_program", err)
	}
	indices = append(indices, index)
	// Decode from pump_event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_event_authority", err)
	}
	indices = append(indices, index)
	// Decode from pump_amm_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_amm_program", err)
	}
	indices = append(indices, index)
	// Decode from amm_event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "amm_event_authority", err)
	}
	indices = append(indices, index)
	// Decode from wsol_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "wsol_mint", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from associated_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "associated_token_program", err)
	}
	indices = append(indices, index)
	// Decode from coin_creator_vault_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "coin_creator_vault_authority", err)
	}
	indices = append(indices, index)
	// Decode from coin_creator_vault_ata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "coin_creator_vault_ata", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *ResetFeeSharingConfigInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 19 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 19, len(indices))
	}
	indexOffset := 0
	// Set new_admin account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_admin", len(accountKeys)-1)
	}
	obj.NewAdmin = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "global", len(accountKeys)-1)
	}
	obj.Global = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sharing_config account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sharing_config", len(accountKeys)-1)
	}
	obj.SharingConfig = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set bonding_curve account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "bonding_curve", len(accountKeys)-1)
	}
	obj.BondingCurve = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_creator_vault account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_creator_vault", len(accountKeys)-1)
	}
	obj.PumpCreatorVault = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_program", len(accountKeys)-1)
	}
	obj.PumpProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_event_authority", len(accountKeys)-1)
	}
	obj.PumpEventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_amm_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_amm_program", len(accountKeys)-1)
	}
	obj.PumpAmmProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set amm_event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "amm_event_authority", len(accountKeys)-1)
	}
	obj.AmmEventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set wsol_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "wsol_mint", len(accountKeys)-1)
	}
	obj.WsolMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set associated_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "associated_token_program", len(accountKeys)-1)
	}
	obj.AssociatedTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set coin_creator_vault_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "coin_creator_vault_authority", len(accountKeys)-1)
	}
	obj.CoinCreatorVaultAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set coin_creator_vault_ata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "coin_creator_vault_ata", len(accountKeys)-1)
	}
	obj.CoinCreatorVaultAta = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *ResetFeeSharingConfigInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.NewAdmin)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Global)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.SharingConfig)
	keys = append(keys, obj.BondingCurve)
	keys = append(keys, obj.PumpCreatorVault)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.PumpProgram)
	keys = append(keys, obj.PumpEventAuthority)
	keys = append(keys, obj.PumpAmmProgram)
	keys = append(keys, obj.AmmEventAuthority)
	keys = append(keys, obj.WsolMint)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.AssociatedTokenProgram)
	keys = append(keys, obj.CoinCreatorVaultAuthority)
	keys = append(keys, obj.CoinCreatorVaultAta)
	return keys
}

// Unmarshal unmarshals the ResetFeeSharingConfigInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *ResetFeeSharingConfigInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling ResetFeeSharingConfigInstruction: %w", err)
	}
	return nil
}

// UnmarshalResetFeeSharingConfigInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalResetFeeSharingConfigInstruction(buf []byte) (*ResetFeeSharingConfigInstruction, error) {
	obj := new(ResetFeeSharingConfigInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RevokeFeeSharingAuthorityInstruction struct{}

func (obj *RevokeFeeSharingAuthorityInstruction) GetDiscriminator() []byte {
	return Instruction_RevokeFeeSharingAuthority[:]
}

// UnmarshalWithDecoder unmarshals the RevokeFeeSharingAuthorityInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RevokeFeeSharingAuthorityInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RevokeFeeSharingAuthorityInstruction", err)
	}
	if discriminator != Instruction_RevokeFeeSharingAuthority {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "RevokeFeeSharingAuthorityInstruction", Instruction_RevokeFeeSharingAuthority, discriminator)
	}
	return nil
}

func (obj *RevokeFeeSharingAuthorityInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	return []uint8{}, nil
}

func (obj *RevokeFeeSharingAuthorityInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	return nil
}

func (obj *RevokeFeeSharingAuthorityInstruction) GetAccountKeys() []solanago.PublicKey {
	return []solanago.PublicKey{}
}

// Unmarshal unmarshals the RevokeFeeSharingAuthorityInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RevokeFeeSharingAuthorityInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RevokeFeeSharingAuthorityInstruction: %w", err)
	}
	return nil
}

// UnmarshalRevokeFeeSharingAuthorityInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRevokeFeeSharingAuthorityInstruction(buf []byte) (*RevokeFeeSharingAuthorityInstruction, error) {
	obj := new(RevokeFeeSharingAuthorityInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type SetAuthorityInstruction struct {
	NewAuthority solanago.PublicKey `json:"new_authority"`

	// Accounts:
	Authority                solanago.PublicKey `json:"authority"`
	AuthorityWritable        bool               `json:"authority_writable"`
	AuthoritySigner          bool               `json:"authority_signer"`
	FeeProgramGlobal         solanago.PublicKey `json:"fee_program_global"`
	FeeProgramGlobalWritable bool               `json:"fee_program_global_writable"`
	EventAuthority           solanago.PublicKey `json:"event_authority"`
	Program                  solanago.PublicKey `json:"program"`
}

func (obj *SetAuthorityInstruction) GetDiscriminator() []byte {
	return Instruction_SetAuthority[:]
}

// UnmarshalWithDecoder unmarshals the SetAuthorityInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *SetAuthorityInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "SetAuthorityInstruction", err)
	}
	if discriminator != Instruction_SetAuthority {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "SetAuthorityInstruction", Instruction_SetAuthority, discriminator)
	}
	// Deserialize `NewAuthority`:
	err = decoder.Decode(&obj.NewAuthority)
	if err != nil {
		return err
	}
	return nil
}

func (obj *SetAuthorityInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from fee_program_global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_program_global", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *SetAuthorityInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_program_global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_program_global", len(accountKeys)-1)
	}
	obj.FeeProgramGlobal = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *SetAuthorityInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.FeeProgramGlobal)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the SetAuthorityInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *SetAuthorityInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling SetAuthorityInstruction: %w", err)
	}
	return nil
}

// UnmarshalSetAuthorityInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalSetAuthorityInstruction(buf []byte) (*SetAuthorityInstruction, error) {
	obj := new(SetAuthorityInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type SetClaimRateLimitInstruction struct {
	ClaimRateLimit uint64 `json:"claim_rate_limit"`

	// Accounts:
	Authority                solanago.PublicKey `json:"authority"`
	AuthorityWritable        bool               `json:"authority_writable"`
	AuthoritySigner          bool               `json:"authority_signer"`
	FeeProgramGlobal         solanago.PublicKey `json:"fee_program_global"`
	FeeProgramGlobalWritable bool               `json:"fee_program_global_writable"`
	EventAuthority           solanago.PublicKey `json:"event_authority"`
	Program                  solanago.PublicKey `json:"program"`
}

func (obj *SetClaimRateLimitInstruction) GetDiscriminator() []byte {
	return Instruction_SetClaimRateLimit[:]
}

// UnmarshalWithDecoder unmarshals the SetClaimRateLimitInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *SetClaimRateLimitInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "SetClaimRateLimitInstruction", err)
	}
	if discriminator != Instruction_SetClaimRateLimit {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "SetClaimRateLimitInstruction", Instruction_SetClaimRateLimit, discriminator)
	}
	// Deserialize `ClaimRateLimit`:
	err = decoder.Decode(&obj.ClaimRateLimit)
	if err != nil {
		return err
	}
	return nil
}

func (obj *SetClaimRateLimitInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from fee_program_global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_program_global", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *SetClaimRateLimitInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_program_global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_program_global", len(accountKeys)-1)
	}
	obj.FeeProgramGlobal = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *SetClaimRateLimitInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.FeeProgramGlobal)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the SetClaimRateLimitInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *SetClaimRateLimitInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling SetClaimRateLimitInstruction: %w", err)
	}
	return nil
}

// UnmarshalSetClaimRateLimitInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalSetClaimRateLimitInstruction(buf []byte) (*SetClaimRateLimitInstruction, error) {
	obj := new(SetClaimRateLimitInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type SetDisableFlagsInstruction struct {
	DisableFlags uint8 `json:"disable_flags"`

	// Accounts:
	Authority                solanago.PublicKey `json:"authority"`
	AuthorityWritable        bool               `json:"authority_writable"`
	AuthoritySigner          bool               `json:"authority_signer"`
	FeeProgramGlobal         solanago.PublicKey `json:"fee_program_global"`
	FeeProgramGlobalWritable bool               `json:"fee_program_global_writable"`
	EventAuthority           solanago.PublicKey `json:"event_authority"`
	Program                  solanago.PublicKey `json:"program"`
}

func (obj *SetDisableFlagsInstruction) GetDiscriminator() []byte {
	return Instruction_SetDisableFlags[:]
}

// UnmarshalWithDecoder unmarshals the SetDisableFlagsInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *SetDisableFlagsInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "SetDisableFlagsInstruction", err)
	}
	if discriminator != Instruction_SetDisableFlags {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "SetDisableFlagsInstruction", Instruction_SetDisableFlags, discriminator)
	}
	// Deserialize `DisableFlags`:
	err = decoder.Decode(&obj.DisableFlags)
	if err != nil {
		return err
	}
	return nil
}

func (obj *SetDisableFlagsInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from fee_program_global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_program_global", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *SetDisableFlagsInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_program_global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_program_global", len(accountKeys)-1)
	}
	obj.FeeProgramGlobal = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *SetDisableFlagsInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.FeeProgramGlobal)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the SetDisableFlagsInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *SetDisableFlagsInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling SetDisableFlagsInstruction: %w", err)
	}
	return nil
}

// UnmarshalSetDisableFlagsInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalSetDisableFlagsInstruction(buf []byte) (*SetDisableFlagsInstruction, error) {
	obj := new(SetDisableFlagsInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type SetSocialClaimAuthorityInstruction struct {
	SocialClaimAuthority solanago.PublicKey `json:"social_claim_authority"`

	// Accounts:
	Authority                solanago.PublicKey `json:"authority"`
	AuthorityWritable        bool               `json:"authority_writable"`
	AuthoritySigner          bool               `json:"authority_signer"`
	FeeProgramGlobal         solanago.PublicKey `json:"fee_program_global"`
	FeeProgramGlobalWritable bool               `json:"fee_program_global_writable"`
	EventAuthority           solanago.PublicKey `json:"event_authority"`
	Program                  solanago.PublicKey `json:"program"`
}

func (obj *SetSocialClaimAuthorityInstruction) GetDiscriminator() []byte {
	return Instruction_SetSocialClaimAuthority[:]
}

// UnmarshalWithDecoder unmarshals the SetSocialClaimAuthorityInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *SetSocialClaimAuthorityInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "SetSocialClaimAuthorityInstruction", err)
	}
	if discriminator != Instruction_SetSocialClaimAuthority {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "SetSocialClaimAuthorityInstruction", Instruction_SetSocialClaimAuthority, discriminator)
	}
	// Deserialize `SocialClaimAuthority`:
	err = decoder.Decode(&obj.SocialClaimAuthority)
	if err != nil {
		return err
	}
	return nil
}

func (obj *SetSocialClaimAuthorityInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from fee_program_global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_program_global", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *SetSocialClaimAuthorityInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_program_global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_program_global", len(accountKeys)-1)
	}
	obj.FeeProgramGlobal = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *SetSocialClaimAuthorityInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.FeeProgramGlobal)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the SetSocialClaimAuthorityInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *SetSocialClaimAuthorityInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling SetSocialClaimAuthorityInstruction: %w", err)
	}
	return nil
}

// UnmarshalSetSocialClaimAuthorityInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalSetSocialClaimAuthorityInstruction(buf []byte) (*SetSocialClaimAuthorityInstruction, error) {
	obj := new(SetSocialClaimAuthorityInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type TransferFeeSharingAuthorityInstruction struct{}

func (obj *TransferFeeSharingAuthorityInstruction) GetDiscriminator() []byte {
	return Instruction_TransferFeeSharingAuthority[:]
}

// UnmarshalWithDecoder unmarshals the TransferFeeSharingAuthorityInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *TransferFeeSharingAuthorityInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "TransferFeeSharingAuthorityInstruction", err)
	}
	if discriminator != Instruction_TransferFeeSharingAuthority {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "TransferFeeSharingAuthorityInstruction", Instruction_TransferFeeSharingAuthority, discriminator)
	}
	return nil
}

func (obj *TransferFeeSharingAuthorityInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	return []uint8{}, nil
}

func (obj *TransferFeeSharingAuthorityInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	return nil
}

func (obj *TransferFeeSharingAuthorityInstruction) GetAccountKeys() []solanago.PublicKey {
	return []solanago.PublicKey{}
}

// Unmarshal unmarshals the TransferFeeSharingAuthorityInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *TransferFeeSharingAuthorityInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling TransferFeeSharingAuthorityInstruction: %w", err)
	}
	return nil
}

// UnmarshalTransferFeeSharingAuthorityInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalTransferFeeSharingAuthorityInstruction(buf []byte) (*TransferFeeSharingAuthorityInstruction, error) {
	obj := new(TransferFeeSharingAuthorityInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateAdminInstruction struct {

	// Accounts:
	Admin             solanago.PublicKey `json:"admin"`
	AdminSigner       bool               `json:"admin_signer"`
	FeeConfig         solanago.PublicKey `json:"fee_config"`
	FeeConfigWritable bool               `json:"fee_config_writable"`
	NewAdmin          solanago.PublicKey `json:"new_admin"`
	ConfigProgramId   solanago.PublicKey `json:"config_program_id"`
	EventAuthority    solanago.PublicKey `json:"event_authority"`
	Program           solanago.PublicKey `json:"program"`
}

func (obj *UpdateAdminInstruction) GetDiscriminator() []byte {
	return Instruction_UpdateAdmin[:]
}

// UnmarshalWithDecoder unmarshals the UpdateAdminInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateAdminInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateAdminInstruction", err)
	}
	if discriminator != Instruction_UpdateAdmin {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "UpdateAdminInstruction", Instruction_UpdateAdmin, discriminator)
	}
	return nil
}

func (obj *UpdateAdminInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from admin account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "admin", err)
	}
	indices = append(indices, index)
	// Decode from fee_config account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_config", err)
	}
	indices = append(indices, index)
	// Decode from new_admin account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_admin", err)
	}
	indices = append(indices, index)
	// Decode from config_program_id account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "config_program_id", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateAdminInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set admin account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "admin", len(accountKeys)-1)
	}
	obj.Admin = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set fee_config account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_config", len(accountKeys)-1)
	}
	obj.FeeConfig = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_admin account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_admin", len(accountKeys)-1)
	}
	obj.NewAdmin = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set config_program_id account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "config_program_id", len(accountKeys)-1)
	}
	obj.ConfigProgramId = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateAdminInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Admin)
	keys = append(keys, obj.FeeConfig)
	keys = append(keys, obj.NewAdmin)
	keys = append(keys, obj.ConfigProgramId)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the UpdateAdminInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateAdminInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateAdminInstruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateAdminInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateAdminInstruction(buf []byte) (*UpdateAdminInstruction, error) {
	obj := new(UpdateAdminInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateFeeConfigInstruction struct {
	FeeTiers []FeeTier `json:"fee_tiers"`
	FlatFees Fees      `json:"flat_fees"`

	// Accounts:
	FeeConfig         solanago.PublicKey `json:"fee_config"`
	FeeConfigWritable bool               `json:"fee_config_writable"`
	Admin             solanago.PublicKey `json:"admin"`
	AdminSigner       bool               `json:"admin_signer"`
	ConfigProgramId   solanago.PublicKey `json:"config_program_id"`
	EventAuthority    solanago.PublicKey `json:"event_authority"`
	Program           solanago.PublicKey `json:"program"`
}

func (obj *UpdateFeeConfigInstruction) GetDiscriminator() []byte {
	return Instruction_UpdateFeeConfig[:]
}

// UnmarshalWithDecoder unmarshals the UpdateFeeConfigInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateFeeConfigInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateFeeConfigInstruction", err)
	}
	if discriminator != Instruction_UpdateFeeConfig {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "UpdateFeeConfigInstruction", Instruction_UpdateFeeConfig, discriminator)
	}
	// Deserialize `FeeTiers`:
	err = decoder.Decode(&obj.FeeTiers)
	if err != nil {
		return err
	}
	// Deserialize `FlatFees`:
	err = decoder.Decode(&obj.FlatFees)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateFeeConfigInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from fee_config account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_config", err)
	}
	indices = append(indices, index)
	// Decode from admin account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "admin", err)
	}
	indices = append(indices, index)
	// Decode from config_program_id account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "config_program_id", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateFeeConfigInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set fee_config account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_config", len(accountKeys)-1)
	}
	obj.FeeConfig = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set admin account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "admin", len(accountKeys)-1)
	}
	obj.Admin = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set config_program_id account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "config_program_id", len(accountKeys)-1)
	}
	obj.ConfigProgramId = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateFeeConfigInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.FeeConfig)
	keys = append(keys, obj.Admin)
	keys = append(keys, obj.ConfigProgramId)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the UpdateFeeConfigInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateFeeConfigInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateFeeConfigInstruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateFeeConfigInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateFeeConfigInstruction(buf []byte) (*UpdateFeeConfigInstruction, error) {
	obj := new(UpdateFeeConfigInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateFeeSharesInstruction struct {
	Shareholders []Shareholder `json:"shareholders"`

	// Accounts:
	EventAuthority                    solanago.PublicKey `json:"event_authority"`
	Program                           solanago.PublicKey `json:"program"`
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	Global                            solanago.PublicKey `json:"global"`
	Mint                              solanago.PublicKey `json:"mint"`
	SharingConfig                     solanago.PublicKey `json:"sharing_config"`
	SharingConfigWritable             bool               `json:"sharing_config_writable"`
	BondingCurve                      solanago.PublicKey `json:"bonding_curve"`
	PumpCreatorVault                  solanago.PublicKey `json:"pump_creator_vault"`
	PumpCreatorVaultWritable          bool               `json:"pump_creator_vault_writable"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	PumpProgram                       solanago.PublicKey `json:"pump_program"`
	PumpEventAuthority                solanago.PublicKey `json:"pump_event_authority"`
	PumpAmmProgram                    solanago.PublicKey `json:"pump_amm_program"`
	AmmEventAuthority                 solanago.PublicKey `json:"amm_event_authority"`
	WsolMint                          solanago.PublicKey `json:"wsol_mint"`
	TokenProgram                      solanago.PublicKey `json:"token_program"`
	AssociatedTokenProgram            solanago.PublicKey `json:"associated_token_program"`
	CoinCreatorVaultAuthority         solanago.PublicKey `json:"coin_creator_vault_authority"`
	CoinCreatorVaultAuthorityWritable bool               `json:"coin_creator_vault_authority_writable"`
	CoinCreatorVaultAta               solanago.PublicKey `json:"coin_creator_vault_ata"`
	CoinCreatorVaultAtaWritable       bool               `json:"coin_creator_vault_ata_writable"`
}

func (obj *UpdateFeeSharesInstruction) GetDiscriminator() []byte {
	return Instruction_UpdateFeeShares[:]
}

// UnmarshalWithDecoder unmarshals the UpdateFeeSharesInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateFeeSharesInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateFeeSharesInstruction", err)
	}
	if discriminator != Instruction_UpdateFeeShares {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "UpdateFeeSharesInstruction", Instruction_UpdateFeeShares, discriminator)
	}
	// Deserialize `Shareholders`:
	err = decoder.Decode(&obj.Shareholders)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateFeeSharesInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from global account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "global", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from sharing_config account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sharing_config", err)
	}
	indices = append(indices, index)
	// Decode from bonding_curve account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "bonding_curve", err)
	}
	indices = append(indices, index)
	// Decode from pump_creator_vault account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_creator_vault", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from pump_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_program", err)
	}
	indices = append(indices, index)
	// Decode from pump_event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_event_authority", err)
	}
	indices = append(indices, index)
	// Decode from pump_amm_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "pump_amm_program", err)
	}
	indices = append(indices, index)
	// Decode from amm_event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "amm_event_authority", err)
	}
	indices = append(indices, index)
	// Decode from wsol_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "wsol_mint", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from associated_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "associated_token_program", err)
	}
	indices = append(indices, index)
	// Decode from coin_creator_vault_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "coin_creator_vault_authority", err)
	}
	indices = append(indices, index)
	// Decode from coin_creator_vault_ata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "coin_creator_vault_ata", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateFeeSharesInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 18 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 18, len(indices))
	}
	indexOffset := 0
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set global account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "global", len(accountKeys)-1)
	}
	obj.Global = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sharing_config account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sharing_config", len(accountKeys)-1)
	}
	obj.SharingConfig = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set bonding_curve account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "bonding_curve", len(accountKeys)-1)
	}
	obj.BondingCurve = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_creator_vault account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_creator_vault", len(accountKeys)-1)
	}
	obj.PumpCreatorVault = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_program", len(accountKeys)-1)
	}
	obj.PumpProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_event_authority", len(accountKeys)-1)
	}
	obj.PumpEventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set pump_amm_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "pump_amm_program", len(accountKeys)-1)
	}
	obj.PumpAmmProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set amm_event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "amm_event_authority", len(accountKeys)-1)
	}
	obj.AmmEventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set wsol_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "wsol_mint", len(accountKeys)-1)
	}
	obj.WsolMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set associated_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "associated_token_program", len(accountKeys)-1)
	}
	obj.AssociatedTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set coin_creator_vault_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "coin_creator_vault_authority", len(accountKeys)-1)
	}
	obj.CoinCreatorVaultAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set coin_creator_vault_ata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "coin_creator_vault_ata", len(accountKeys)-1)
	}
	obj.CoinCreatorVaultAta = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateFeeSharesInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Global)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.SharingConfig)
	keys = append(keys, obj.BondingCurve)
	keys = append(keys, obj.PumpCreatorVault)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.PumpProgram)
	keys = append(keys, obj.PumpEventAuthority)
	keys = append(keys, obj.PumpAmmProgram)
	keys = append(keys, obj.AmmEventAuthority)
	keys = append(keys, obj.WsolMint)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.AssociatedTokenProgram)
	keys = append(keys, obj.CoinCreatorVaultAuthority)
	keys = append(keys, obj.CoinCreatorVaultAta)
	return keys
}

// Unmarshal unmarshals the UpdateFeeSharesInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateFeeSharesInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateFeeSharesInstruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateFeeSharesInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateFeeSharesInstruction(buf []byte) (*UpdateFeeSharesInstruction, error) {
	obj := new(UpdateFeeSharesInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpsertFeeTiersInstruction struct {
	FeeTiers []FeeTier `json:"fee_tiers"`
	Offset   uint8     `json:"offset"`

	// Accounts:
	FeeConfig         solanago.PublicKey `json:"fee_config"`
	FeeConfigWritable bool               `json:"fee_config_writable"`
	Admin             solanago.PublicKey `json:"admin"`
	AdminSigner       bool               `json:"admin_signer"`
	ConfigProgramId   solanago.PublicKey `json:"config_program_id"`
	EventAuthority    solanago.PublicKey `json:"event_authority"`
	Program           solanago.PublicKey `json:"program"`
}

func (obj *UpsertFeeTiersInstruction) GetDiscriminator() []byte {
	return Instruction_UpsertFeeTiers[:]
}

// UnmarshalWithDecoder unmarshals the UpsertFeeTiersInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpsertFeeTiersInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadDiscriminator()
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpsertFeeTiersInstruction", err)
	}
	if discriminator != Instruction_UpsertFeeTiers {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %s, got %s", "UpsertFeeTiersInstruction", Instruction_UpsertFeeTiers, discriminator)
	}
	// Deserialize `FeeTiers`:
	err = decoder.Decode(&obj.FeeTiers)
	if err != nil {
		return err
	}
	// Deserialize `Offset`:
	err = decoder.Decode(&obj.Offset)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpsertFeeTiersInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from fee_config account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "fee_config", err)
	}
	indices = append(indices, index)
	// Decode from admin account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "admin", err)
	}
	indices = append(indices, index)
	// Decode from config_program_id account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "config_program_id", err)
	}
	indices = append(indices, index)
	// Decode from event_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "event_authority", err)
	}
	indices = append(indices, index)
	// Decode from program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpsertFeeTiersInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set fee_config account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "fee_config", len(accountKeys)-1)
	}
	obj.FeeConfig = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set admin account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "admin", len(accountKeys)-1)
	}
	obj.Admin = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set config_program_id account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "config_program_id", len(accountKeys)-1)
	}
	obj.ConfigProgramId = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set event_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "event_authority", len(accountKeys)-1)
	}
	obj.EventAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program", len(accountKeys)-1)
	}
	obj.Program = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpsertFeeTiersInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.FeeConfig)
	keys = append(keys, obj.Admin)
	keys = append(keys, obj.ConfigProgramId)
	keys = append(keys, obj.EventAuthority)
	keys = append(keys, obj.Program)
	return keys
}

// Unmarshal unmarshals the UpsertFeeTiersInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpsertFeeTiersInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpsertFeeTiersInstruction: %w", err)
	}
	return nil
}

// UnmarshalUpsertFeeTiersInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpsertFeeTiersInstruction(buf []byte) (*UpsertFeeTiersInstruction, error) {
	obj := new(UpsertFeeTiersInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

// Instruction interface defines common methods for all instruction types
type Instruction interface {
	GetDiscriminator() []byte

	UnmarshalWithDecoder(decoder *binary.Decoder) error

	UnmarshalAccountIndices(buf []byte) ([]uint8, error)

	PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error

	GetAccountKeys() []solanago.PublicKey
}

// ParseInstruction parses instruction data and optionally populates accounts
// If accountIndicesData is nil or empty, accounts will not be populated
func ParseInstruction(instructionData []byte, accountIndicesData []byte, accountKeys []solanago.PublicKey) (Instruction, error) {
	// Validate inputs
	if len(instructionData) < 8 {
		return nil, fmt.Errorf("instruction data too short: expected at least 8 bytes, got %d", len(instructionData))
	}
	// Extract discriminator
	discriminator := [8]byte{}
	copy(discriminator[:], instructionData[0:8])
	// Parse based on discriminator
	switch discriminator {
	case Instruction_ClaimSocialFeePda:
		instruction := new(ClaimSocialFeePdaInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as ClaimSocialFeePdaInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CrankDonationFeePda:
		instruction := new(CrankDonationFeePdaInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CrankDonationFeePdaInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateDonationFeePda:
		instruction := new(CreateDonationFeePdaInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateDonationFeePdaInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateFeeSharingConfig:
		instruction := new(CreateFeeSharingConfigInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateFeeSharingConfigInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateSocialFeePda:
		instruction := new(CreateSocialFeePdaInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateSocialFeePdaInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_GetFees:
		instruction := new(GetFeesInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as GetFeesInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_InitializeFeeConfig:
		instruction := new(InitializeFeeConfigInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as InitializeFeeConfigInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_InitializeFeeProgramGlobal:
		instruction := new(InitializeFeeProgramGlobalInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as InitializeFeeProgramGlobalInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_ResetFeeSharingConfig:
		instruction := new(ResetFeeSharingConfigInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as ResetFeeSharingConfigInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RevokeFeeSharingAuthority:
		instruction := new(RevokeFeeSharingAuthorityInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RevokeFeeSharingAuthorityInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_SetAuthority:
		instruction := new(SetAuthorityInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as SetAuthorityInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_SetClaimRateLimit:
		instruction := new(SetClaimRateLimitInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as SetClaimRateLimitInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_SetDisableFlags:
		instruction := new(SetDisableFlagsInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as SetDisableFlagsInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_SetSocialClaimAuthority:
		instruction := new(SetSocialClaimAuthorityInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as SetSocialClaimAuthorityInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_TransferFeeSharingAuthority:
		instruction := new(TransferFeeSharingAuthorityInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as TransferFeeSharingAuthorityInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateAdmin:
		instruction := new(UpdateAdminInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateAdminInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateFeeConfig:
		instruction := new(UpdateFeeConfigInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateFeeConfigInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateFeeShares:
		instruction := new(UpdateFeeSharesInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateFeeSharesInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpsertFeeTiers:
		instruction := new(UpsertFeeTiersInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpsertFeeTiersInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	default:
		return nil, fmt.Errorf("unknown instruction discriminator: %s", binary.FormatDiscriminator(discriminator))
	}
}

// ParseInstructionTyped parses instruction data and returns a specific instruction type // T must implement the Instruction interface
func ParseInstructionTyped[T Instruction](instructionData []byte, accountIndicesData []byte, accountKeys []solanago.PublicKey) (T, error) {
	instruction, err := ParseInstruction(instructionData, accountIndicesData, accountKeys)
	if err != nil {
		return *new(T), err
	}
	typed, ok := instruction.(T)
	if !ok {
		return *new(T), fmt.Errorf("instruction is not of expected type")
	}
	return typed, nil
}

// ParseInstructionWithoutAccounts parses instruction data without account information
func ParseInstructionWithoutAccounts(instructionData []byte) (Instruction, error) {
	return ParseInstruction(instructionData, nil, []solanago.PublicKey{})
}

// ParseInstructionWithAccounts parses instruction data with account information
func ParseInstructionWithAccounts(instructionData []byte, accountIndicesData []byte, accountKeys []solanago.PublicKey) (Instruction, error) {
	return ParseInstruction(instructionData, accountIndicesData, accountKeys)
}
